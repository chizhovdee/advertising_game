<h1><%= I18n.t('home.title') %></h1>

<div class="tabs">
  <% for tab in @tabs: %>
    <div class="tab <%= tab %> <%= 'current' if @currentTab == tab %>" data-type="<%= tab %>">
      <%= I18n.t("home.tabs.#{ tab }") %>
    </div>
  <% end %>
</div>

<div class="tab_content"></div>